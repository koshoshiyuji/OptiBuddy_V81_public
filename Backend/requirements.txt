# 2026-08-12: OptiBuddy本体はApache License 2.0でOSS配布。
# IBM CPLEX/CP Optimizer(cplex, docplex)は別ライセンスのため、
# 本ファイルには含めない(requirements-cplex.txtを参照)。
# 本ファイルだけのpip installでもFlaskは起動し、CPLEXを使わないドメイン
# (TruckDispatcherの貪欲法フォールバック等)やMIP系ドメインのHiGHS
# フォールバック(ortools内蔵HiGHS経由、2026-09-01よりhighspy単体パッケージは
# 不要。ce_limit_mip_fallback.py参照)は動作する。ただし現時点では
# CP Optimizer系ドメインの大半はcplex/docplexが無いと動かない
# (docs/DESIGN_2026-08-12_cp_sat_backend_support.md参照、OR-Tools CP-SAT
# 対応で解消予定)。
#
# cpmpy/ortools(下記6行)は Backend/tools/pilot_cpmpy_*.py のCPMpyパイロット
# 検証用。Apache 2.0のみでCPLEX不要、OSS版base installに含めて問題ない。
# ortoolsは最新(9.15系)を使用。ortools>=9.15はnumpy>=2.0.2を要求するため、
# 本ファイルのnumpyを1.26.4→2.4.6へ更新した(下記numpy行参照)。
# Koshoshiの開発・本番環境(macOS、開発=本番)でこの組み合わせ
# (numpy==2.4.6 + cpmpy==1.0.0 + ortools==9.15.6755)の動作を実機確認済み
# (2026-09-01時点、highspyは依存関係から除外済み。下記numpy行のコメントも参照)。
#   - cpmpy 1.0.0はortools 9.9.x/9.10.x/9.11.x を明示的に除外している
#     (cpmpy側の既知の非互換)。9.15.6755はこの除外範囲外。
# 現時点ではまだregistry.py等の本番経路には未接続(パイロットのみ)。
absl-py==2.5.0
cpmpy==1.0.0
immutabledict==4.3.1
ortools==9.15.6755
pandas==2.3.3
# 2026-08-21修正: 6.31.1のままだとクリーンな`pip install -r requirements.txt`が
# ResolutionImpossibleで失敗する(ortools==9.15.6755がprotobuf>=6.33.1,<6.34を
# 要求するため、6.31.1と衝突する)。Koshoshiの既存環境では別経路(ortools単体の
# 後からのアップグレード等)でprotobufが6.33系に上がっていたため気づかれて
# いなかった不整合。GitHub Actions CI導入(.github/workflows/backend-tests.yml)の
# クリーンインストール検証で発覚し修正。6.33.6はortoolsの要求範囲内の最新版。
protobuf==6.33.6
pytz==2026.3.post1
tzdata==2026.3
annotated-types==0.7.0
anthropic==0.104.1
anyio==4.11.0
blinker==1.9.0
cachetools==6.2.0
certifi==2025.8.3
chardet==5.2.0
charset-normalizer==3.4.3
click==8.3.0
colorama==0.4.6
contourpy==1.3.2
cycler==0.12.1
distlib==0.4.0
distro==1.9.0
docstring_parser==0.18.0
exceptiongroup==1.3.0
filelock==3.19.1
Flask==3.1.3
flask-cors==6.0.1
fonttools==4.63.0
h11==0.16.0
# 2026-09-01: highspy==1.15.1のピンを削除した。CE上限MIPフォールバック
# (Backend/solvers/base/ce_limit_mip_fallback.py の_solve_via_highs())が、
# 単体highspyパッケージからortools内蔵のHiGHSバックエンド
# (ortools.linear_solver.python.model_builder)に切り替わったため、
# highspy自体がもう不要になった。単体highspyとortools内蔵HiGHSは別ビルドの
# 同一ライブラリで、同一プロセス内で両方読み込まれるとdlopenのシンボル
# 解決エラーやHiGHSワーカースレッド内のsegfaultを引き起こすことが判明した
# ため(PortfolioOverlapDesigner→MysteryShopperScheduler segfault、
# MysteryShopperScheduler→NursingWorkloadBalance ImportError、両方2026-08-31
# 〜09-01に実機確認)、highspyを依存関係から完全に外すことでこの衝突自体を
# なくした。旧highspy==1.14.0→1.15.1の切替やLinux固有ABI問題の経緯は
# 過去のコメント参照だったが、highspy自体が不要になったため経緯ごと削除。
httpcore==1.0.9
httpx==0.28.1
idna==3.10
iniconfig==2.1.0
itsdangerous==2.2.0
Jinja2==3.1.6
jiter==0.11.0
joblib==1.5.3
jsonpatch==1.33
jsonpointer==3.0.0
kiwisolver==1.4.9
lxml==6.1.1
markdown-it-py==4.2.0
MarkupSafe==3.0.2
matplotlib==3.10.6
mdurl==0.1.2
networkx==3.4.2
# 2026-08-12(4): 1.26.4→2.4.6。ortools>=9.15がnumpy>=2.0.2を要求するため
# 更新(cpmpy/ortools追加の経緯はファイル冒頭のコメント参照)。matplotlib等
# 既存の他numpy依存パッケージも2.xで動作することをクリーンなvenvへの
# 全量pip installで確認済み。Koshoshiの開発=本番環境(macOS)でも
# numpy==2.4.6 + ortools最新の組み合わせを実機確認済み(2026-09-01時点、
# highspyは依存関係から除外済み)。
numpy==2.4.6
openai==1.108.2
packaging==25.0
pillow==11.3.0
platformdirs==4.4.0
pluggy==1.6.0
pydantic==2.11.9
pydantic_core==2.33.2
pydeps==3.0.2
Pygments==2.19.2
pyparsing==3.2.4
pyproject-api==1.9.1
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
python-pptx==1.0.2
PyYAML==6.0.2
regex==2026.5.9
requests==2.34.2
rich==15.0.0
shellingham==1.5.4
six==1.17.0
sniffio==1.3.1
stdlib-list==0.12.0
tomli==2.2.1
tqdm==4.67.1
typing-inspection==0.4.1
typing_extensions==4.15.0
urllib3==2.7.0
Werkzeug==3.1.8
xlsxwriter==3.2.9
